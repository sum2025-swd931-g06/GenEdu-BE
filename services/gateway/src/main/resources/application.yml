spring:
  config:
    import: optional:configserver:http://localhost:8888
  application:
    name: gateway-service


springdoc:
  swagger-ui:
    oauth:
      client-id: genedu-be
      client-secret: kniGc5Z7lwdgtOjBuwfrNIIEpYRia9Lb
    csrf:
      enabled: false